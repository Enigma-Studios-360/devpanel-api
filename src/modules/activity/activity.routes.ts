// Activity endpoints are mounted nested under /api/teams/:teamId/activity and
// /api/projects/:projectId/activity in their respective route files. This file
// is kept as a placeholder for future global activity endpoints (admin only).
import { Router } from 'express';

export const activityRouter = Router();
